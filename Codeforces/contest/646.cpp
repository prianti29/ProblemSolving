#include<bits/stdc++.h>
using namespace std;

typedef long long ll;
#define speed() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
int main()
{
    speed();
    ll a,b,t,n,m;
    vector<ll>v;
    cin>>t;
    while(t--){
            ll sum=0;
        cin>>n>>m;
        ll arr[n];
        for(i=0;i<n;i++)
            cin>>arr[i];
            if(i==arr[i])
         for(int i = 0; i<n ; i++){
      sum+=arr[i];
   }
   if()
        //v.push_back(n);

    }
}
