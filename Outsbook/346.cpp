#include<bits/stdc++.h>
using namespace std;

int main()
{

    /* double j,n,k,m,o,a,b,c;
      while(cin>>a>>b>>c)
      {
        if(b>=a && a>=c || c>=a && a>=b)
          cout<<a*a<<endl;
        else if( a>=b && b>=c || c>=b && b>=a )
          cout<<b*b<<endl;
        else
          cout<<c*c<<endl;
      }*/
    /*long long arr[4],k,i,n,a,b,c;
    vector<long long>v;
    while(cin>>a>>b>>c)
    {
        //cin>>k;
        v.push_back(a);
        v.push_back(b);
        v.push_back(c);
        sort(v.begin(),v.end());
        cout<<v[1]*v[1]<<endl;
        v.clear();
    }*/
}

